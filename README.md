# skill-policy-hub-workflow-sandbox

Public sandbox repository used to validate collaborative git workflow artifacts and remote GitHub policy tests generated by `skill-policy-hub`.

This repository exists only for automated and manual testing of the opinionated collaborative Git workflow.
It is expected to contain ephemeral branches, pull requests, workflow runs, and other disposable test artifacts.
